package nengine.physics.dynamics.contacts;

class ContactFactory
{
    private var allocator:
    public function new(allocator:)
    {
        this.allocator = allocator;
        initializeRegisters();
    }

    private function initializeRegisters():
    {
    }
}
